import streamlit as st
import pandas as pd
import pickle
import sklearn
import zipfile
import io

# Загрузка модели
@st.cache_resource
def load_model():
    with zipfile.ZipFile('rfc_classifier.zip', 'r') as zip_ref:
        model_files = [f for f in zip_ref.namelist() if f.endswith('.pkl')]
        model_file = model_files[0]
        with zip_ref.open(model_file) as f:
            model = pickle.load(f)
            return model

model = load_model()

st.title ("🔮Предсказание оттока клиентов")

# Drug & Drop загрузка файла
uploaded_file = st.file_uploader("Перетащите сюда файл Excel/csv с данными клиента",
                                type=['xlsx', 'csv'])

df = None # Инициализация df вне блока if uploaded_file, для обхода NameError

if uploaded_file:
    if uploaded_file.name.endswith('.xlsx'):
        df = pd.read_excel(uploaded_file)
    elif uploaded_file.name.endswith('.csv'): 
        df = pd.read_csv(uploaded_file)
    else:
        pass
    st.write("Данные загружены!")
    
    # Предобразование данных для модели
    df['Date'] = pd.to_datetime(df['Date'])
    df['Day'] = df['Date'].dt.day
    df['Month'] = df['Date'].dt.month
    df['Year'] = df['Date'].dt.year
    
    new_names = ["ИД", "АЗС", "Оператор", "Сопутствующий товар", "Нефтепродукт", "Вид оплаты", "Тип транзакции",
              "Контрагент", "№ бонусной карты", "№ чека", "Цена", "Скидка", "Бонусов начислено", "Бонусов списано",
              "Остаток бонусов", "Кол-во (только для СТ)", "Объем (только для НП)", "Масса (только для НП)",
              "Время суток", "Дата", "День", "Месяц", "Год"]
    # Переименование столбцов
    df.columns = new_names

    # Представим время транзакции в секундах, это нужно для выражения данных этого столбца в цисленных значениях, а не типа "datetime"
    df['Время суток секунды'] = df['Время суток'].apply(lambda x: int(x.split(':')[0]) * 3600
                                                                               + int(x.split(':')[1]) * 60 + int(x.split(':')[2]))
    df['Время суток часы'] = df['Время суток секунды'] / 3600
    df.drop('Время суток', axis=1, inplace=True)

    # Отбор признаков для модели и переименование в соответствии с моделью
    df = df[['АЗС', 'Оператор', 'Цена', 'Нефтепродукт', 'Вид оплаты', 'Тип транзакции', 'Объем (только для НП)', 'Время суток часы']]
    df.columns = ['Последний(ая) АЗС перед оттоком', 'Последний(ая) Оператор перед оттоком', 'Последний(ая) Цена перед оттоком', 'Последний(ая) Нефтепродукт перед оттоком',
              'Последний(ая) Вид оплаты перед оттоком', 'Последний(ая) Тип транзакции перед оттоком', 'Последний(ая) Объем (только для НП) перед оттоком',
              'Последний(ая) Время суток часы перед оттоком']
    
    st.dataframe(df)
   
# Предсказание
if st.button("Предсказать отток"):
    if df is None:
        st.warning("⚠️ Сначала загрузите данные!")
    else:
        try:
            predictions = model.predict(df)
            probabilities = model.predict_proba(df)

            # Добавляем результаты в DataFrame
            results = df.copy()
            results['Прогноз оттока'] = predictions
            results['Вероятность оттока'] = probabilities[:, 1]  # Вероятность класса "1" (отток)
            results['Вероятность удержания'] = probabilities[:, 0]

            # Красивый вывод: заменяем 1/0 на понятные метки
            results['Статус'] = results['Прогноз оттока'].map({1: 'Отток 🆘', 0: 'Удержание ✅'})

            st.write("### Результаты предсказания:")
            st.dataframe(
                results[['Статус', 'Вероятность оттока']].style.format({
                    'Вероятность оттока': '{:.2%}'
                }).apply(lambda row: ['background-color: #ffcccc' if row['Статус'] == 'Отток 🆘' else 'background-color: #ccffcc' for _ in row], axis=1)
            )

            # Дополнительно: статистика по оттоку
            total = len(results)
            churn_count = (results['Прогноз оттока'] == 1).sum()
            st.info(f"📈 Из {total} клиентов предсказан отток у {churn_count} ({churn_count / total:.2%})")
        except Exception as e:
            st.error(f"❌ Ошибка при выполнении предсказания: {e}")































